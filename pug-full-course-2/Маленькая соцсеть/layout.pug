doctype html
html
    head
        meta(charset="UTF-8")
        title= name
        link(href="../style.css", rel="stylesheet")

    body
        .content-wrapper
            h2 #{name} #[span.age(title="Дата рождения: " + birth)= calcAge(birth)]
            blockquote= slogan
            p= bio